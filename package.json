{
  "name": "livemore",
  "version": "1.0.0",
  "description": "Live more. Scroll less.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "rm -rf dist && cp -r public dist"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  }
}
